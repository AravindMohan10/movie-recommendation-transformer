[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra --strict-markers
markers =
    integration: needs running backend or heavy model artifacts
    slow: loads ML checkpoints / torch
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
