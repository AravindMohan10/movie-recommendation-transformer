# CineAI Frontend - Environment Variables
# Copy to .env for local dev. For production builds, use .env.production (or set at build time).

# Production: set to your backend API URL (used when VITE_API_BASE is set in .env.production or build)
VITE_API_BASE=https://your-api.example.com/api
